# Stack operations in AlgoSpeak
# Demonstrates the built-in stack data structure

create stack s

push 10 into s
push 20 into s
push 30 into s

# Pop and display: should show 30, 20, 10 (LIFO order)
show pop from s
show pop from s
show pop from s
